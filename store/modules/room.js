export default {
	namespaced: true,

	state: {
		roomInfo: {}
	},

	mutations: {
		// getRoomList(state, roomInfo) {
		// 	state.roomInfo = roomInfo
		// 	console.log(state.roomInfo, roomInfo);
		// }
	}
}
